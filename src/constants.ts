/**
 * Configuration constants
 * Centralized place to edit default values and settings
 */

export const constants = {
  // Default directory for architecture files
  defaultOutDir: '.archctl',

  // Config file name
  configFileName: 'archctl.config.json',

  // Default config values
  defaultConfig: {
    language: '',
    framework: '',
    testing: '',
  },
};
